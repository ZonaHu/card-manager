# Frontend Environment Variables

# API Base URL - points to the backend. Defaults to http://localhost:3001 if unset.
# Set this for production deployments where frontend and backend are on separate origins.
VITE_API_BASE_URL=http://localhost:3001

# Environment mode
# VITE_NODE_ENV=development
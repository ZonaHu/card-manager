# Frontend Environment Variables (Optional)
# Currently not used, but reserved for future frontend-specific config

# API Base URL (if deploying frontend and backend separately)
# VITE_API_BASE_URL=http://localhost:3001

# Environment mode
# VITE_NODE_ENV=development